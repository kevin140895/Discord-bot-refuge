# Keep discord.py stable while tracking its current upstream voice dependency range.
# discord.py 2.7.1 still publishes voice with PyNaCl<1.6, but upstream master
# accepts PyNaCl>=1.6.0,<1.7. Pin >=1.6.2 to include the libsodium security fix.
discord.py>=2.7,<3
PyNaCl>=1.6.2,<1.7
davey>=0.1.0
python-dotenv>=1.0,<2
Pillow>=10.0,<13
yt-dlp[default]>=2026.7.4
imageio-ffmpeg>=0.4,<1
aiohttp>=3.8,<4
tzdata>=2024.1
