# Direct runtime dependencies.
#
# Edit this file to change dependency policy. requirements.txt is generated by
# scripts/compile_requirements.py and is the file installed in production.
#
# Keep discord.py without the [voice] extra for now: the project deliberately
# carries a patched PyNaCl range until the released discord.py voice metadata
# accepts PyNaCl 1.6.x.
discord.py>=2.7,<3
PyNaCl>=1.6.2,<1.7
davey>=0.1.0
python-dotenv>=1.0,<2
Pillow>=10.0,<13
yt-dlp[default]>=2025.11.12
imageio-ffmpeg>=0.4,<1
aiohttp>=3.8,<4
tzdata>=2024.1
